<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Usa un tema NoActionBar -->
    <style name="Base.Theme.Train2gether" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Rende il colore della barra superiore scuro/nero come lo sfondo -->
        <item name="android:statusBarColor">#121212</item>

        <!-- Mantiene le icone di stato (ora, batteria, wifi) chiare/bianche -->
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="Theme.Train2gether" parent="Base.Theme.Train2gether" />
</resources>